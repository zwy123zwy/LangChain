# 本后端服务端口（默认 2024，agent-ui 直接连此地址即可）
# PORT=2024

# 本地 Ollama（LangChain 文件系统 Agent 使用）
# OLLAMA_BASE_URL=http://localhost:11434

# LangGraph 实际服务地址（本后端会代理过去，默认 8123）
# LANGGRAPH_API_URL=http://localhost:8123
# LANGGRAPH_API_KEY=   # 若部署需要 API Key 则填写
# LANGGRAPH_DEFAULT_ASSISTANT_ID=   # 可选

# 若改用 OpenAI 等（仅部分 demo 使用）
# OPENAI_API_KEY=sk-your-openai-api-key-here
